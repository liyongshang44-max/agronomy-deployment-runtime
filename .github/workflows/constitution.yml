name: ADR Constitution

on:
  push:
    branches:
      - "**"
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  constitutional-boundary:
    name: Constitutional boundary acceptance
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          package-manager-cache: false

      - name: Report runtime
        run: node --version

      - name: Constitutional static checks
        run: npm run build

      - name: RA02 pre-effect rights enforcement acceptance
        run: node acceptance/rights-enforcement/run.mjs

      - name: Constitutional acceptance
        run: npm test
